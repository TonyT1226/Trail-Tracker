{
  "name": "at-tracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Appalachian Trail progress dashboard (static site for GitHub Pages)",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "test": "node --test \"tests/*.test.mjs\"",
    "test:py": "python3 -m unittest tests/test_pipeline.py -v"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
