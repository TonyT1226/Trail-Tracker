# Trail Tracker 项目计划

> 状态：草案 v0.1　最后更新：2026-09-20
> 这份文档放在仓库里，随项目一起更新。所有已做出的决定都记在第 10 节。

## 1. 项目是什么

一个开源的长距离徒步记录工具。用户在地图上记录自己走过的路线，查看进度、里程、爬升和历史轨迹。

先从 Appalachian Trail（AT）开始，之后加入 PCT 等经典长线，再支持导入 GPX 等轨迹文件，最后推出 iOS / iPadOS / macOS 原生 App 并上架 App Store。

## 2. 做什么，不做什么

**做**
- 路线进度记录与可视化
- 历史轨迹在地图上的显示（全部显示或分条显示）
- 里程、爬升、天数等统计
- 导入 GPX 等轨迹文件
- 用图片分享进度

**不做**
- 路线导航、实时路况、路线规划
- 服务器、用户账号、云端数据库（近期不做，见第 5 节）
- 关注、动态流、评论、排行榜等需要后端的社交功能（暂不承诺）
- 从 AllTrails 等网站的链接直接抓取路线（没有公开接口，且服务条款限制，用户可以自己导出 GPX 再导入）

## 3. 原则

1. 只记录，不导航。
2. 本地优先：数据留在用户自己的设备上，没有服务器，没有账号。
3. 位置数据属于隐私数据，默认不上传，不放进公开仓库。
4. 数据格式开放，带版本号，网页和 iOS 共用同一套结构。
5. 每个版本结束时都是一个能独立使用的成品，中途停下也不算白做。
6. 路线数据只使用许可清楚的来源，并注明出处。

## 4. 路线图

### 1.0　把 AT 做好

功能
- AT 参考线（GeoJSON），带里程标
- 在地图上显示已走区间和当前位置
- 进度统计：总里程、完成百分比、剩余里程、累计天数、平均每日里程
- 海拔剖面
- 备份导出与导入
- 数据格式 v1 定稿

完成标准
- [ ] 能在地图上完整看到 AT，并能切换查看任意里程位置
- [ ] 能录入已走区间并看到进度更新
- [ ] 关闭再打开页面，数据仍在
- [ ] 导出的备份文件可以在另一台设备上导入并恢复
- [ ] 路线数据的来源、年份和许可写在文档里
- [ ] README 写明如何下载并运行

### 2.0　多条长线与全局总览

功能
- 路线抽象：每条路线是一个独立的数据包（名称、参考线、里程标、颜色、来源）
- 路线切换
- 加入 PCT，并预留 CDT、Long Trail 等
- 全局总览：一张地图显示所有路线及各自完成百分比
- 一份"如何添加新路线"的说明，方便社区贡献数据

完成标准
- [ ] 新增一条路线只需要新增数据，不需要改页面代码
- [ ] AT 和 PCT 都能独立记录进度
- [ ] 总览页能同时显示多条路线的完成情况

### 3.0　导入 GPX 与历史记录

3.0
- 导入 GPX（TCX、KML、FIT 视需要再加）
- 轨迹清洗：去除 GPS 漂移和暂停点
- 在地图上全部显示或分条显示，可按颜色或年份区分
- 基础统计：距离、累计爬升、日期
- 数据保存在浏览器本地，并纳入备份

3.1
- 把轨迹吸附到参考线上，自动判断走的是哪一段、里程多少
- 显示置信度，吸附不准的可以手动修正

完成标准
- [ ] 用几份不同来源的真实 GPX 文件测试通过
- [ ] 同一批文件多次导入结果一致
- [ ] 距离和爬升与常见手表或 App 的数字在合理误差内

### 4.0　iOS / iPadOS / macOS App

- SwiftUI 多平台单工程
- 数据：SwiftData 加 CloudKit 私有数据库（数据存在用户自己的 iCloud）
- 功能优先级
  1. 查看与导入轨迹，显示进度
  2. 从 Apple 健康导入 Apple Watch 的徒步路线
  3. 后台录制轨迹
  4. 离线地图
- 与网页版共用数据格式，并用同一批测试文件验证算法结果一致

上架前置条件
- [ ] 一台 Mac（Xcode 只能在 Mac 上运行）
- [ ] Apple Developer Program（每年 99 美元）
- [ ] 隐私政策页面与隐私营养标签
- [ ] 后台定位用途说明
- [ ] TestFlight 内测
- [ ] 截图、图标、支持网址

### 之后（暂不承诺）

社交相关功能按成本从低到高分三步，前一步有真实需求再做下一步。
1. 生成进度分享图，通过系统分享发出，不需要后端。
2. 借助 CloudKit 共享，把一段行程分享给少数朋友。
3. 自建后端（关注、动态流等）。真要做时使用现成的托管服务，不自己写服务器。App Store 对有账号和用户内容的 App 要求更多：需要支持 App 内删除账号、提供第三方登录时要同时提供 Apple 登录、需要举报和屏蔽机制。位置分享还要考虑被人跟踪的风险，默认应延迟公开并模糊位置。

## 5. 为什么近期不做服务器

项目定位是开源、由用户自己下载使用。带账号的云端数据库只有两条路，要么每个使用者自己搭后端，要么由项目维护者运营公共服务并承担成本、安全和别人位置数据的隐私责任。两条都不适合当前阶段。iOS 阶段用 iCloud 同步个人数据，占用的是用户自己的空间，维护者也看不到用户数据。

已知代价：iCloud 只覆盖苹果生态；网页版与 App 之间不自动同步，通过导出和导入文件互通。

## 6. 数据格式草案

以下是草案，1.0 开始时逐项确认。

三类数据：Trail（路线）、Milepost（里程标）、Activity（活动记录）。

公共约定
- 每条记录有不会变化的唯一编号（UUID）和格式版本号 `schemaVersion`
- 时间使用 ISO 8601（UTC）
- 距离内部统一用米存储，显示时按用户选择换算成英里或公里
- 坐标沿用 GeoJSON 的 `[经度, 纬度, 海拔]`

Trail
- `id`、`name`、`region`
- `sourceName`、`sourceYear`、`license`（数据来源、年份、许可）
- `lengthMeters`
- `geometry`（GeoJSON LineString）
- `mileposts`

Activity
- `id`、`trailId`（可为空）
- `startTime`、`endTime`
- `source`：`manual`（手动录入区间）、`gpx`、`healthkit`
- `track`（GeoJSON，手动录入时为空）
- `range`（手动录入时使用：起点里程、终点里程）
- `stats`：距离、累计爬升等
- `note`

## 7. 风险与注意事项

- **路线数据许可**：AT 的官方总里程会因改道每年略有变化，数据要标注来源和年份。如果使用 OpenStreetMap 数据，需要按 ODbL 署名。
- **地图令牌**：开源仓库里不能放 Mapbox 令牌，每个使用者填自己的，放在不上传到仓库的配置文件里。
- **隐私**：用户的轨迹不进仓库，仓库里用 `.gitignore` 排除本地数据文件；测试用的 GPX 使用脱敏后的样本。
- **范围膨胀**：每个版本只做本版本的内容，新想法先记入"之后"。
- **吸附算法边界情况**：往返走同一段、支线、路线重叠区段，单独在 3.1 处理。
- **爬升算法差异**：海拔数据有噪声，不同设备算出的爬升不同，需要平滑处理并说明口径。
- **开源协议与 App Store**：强开源协议（如 GPL）与 App Store 分发条款有冲突，倾向宽松协议，或只开源网页部分。协议要在接受外部贡献之前定下。
- **竞品**：FarOut、AllTrails、Strava 等各占一块，本项目只做长线徒步的记录与可视化，不碰导航。

## 8. 协作方式

- 代码由 Claude 编写，项目负责人负责决定要什么、验收好不好用。
- 每个任务拆得足够小，写清验收标准，做完在浏览器里就能看到效果。
- 每个决定都记入第 10 节，换对话或隔一段时间再回来，不需要重新解释。
- 已确认的决定不在后续对话中随意推翻，需要改时在决策记录里新增一条并写明原因。

## 9. 近期待办

1. 盘点现有网站的状态，列出 1.0 还差什么。
2. 确认第 11 节的待定事项。
3. 调研并选定 AT 路线数据来源，确认许可。
4. 定稿数据格式 v1。
5. 整理仓库：README、LICENSE、`.gitignore`、目录结构。
6. 开始 1.0 的第一个小任务。

## 10. 决策记录

| 日期 | 决定 | 原因 |
|---|---|---|
| 2026-09-20 | 产品只做路线记录，不做导航 | 避免与 FarOut 等导航产品正面竞争，范围也更小 |
| 2026-09-20 | 网页分三个版本：1.0 AT；2.0 多路线与全局总览；3.0 导入 GPX 与历史记录 | 每个版本都能独立使用 |
| 2026-09-20 | 三个网页版本完成后再开始 iOS 开发 | 先把数据格式和算法在网页上验证清楚 |
| 2026-09-20 | 不使用 GitHub Pages，做成开源仓库，由使用者自行下载使用 | 项目定位是开源工具 |
| 2026-09-20 | 近期不做服务器和账号系统 | 见第 5 节 |
| 2026-09-20 | iOS 阶段用 iCloud 保存个人数据 | 不需要自建后端，用户数据归用户自己 |
| 2026-09-20 | 不做 AllTrails 链接导入，改为支持导入 GPX 等文件 | 没有公开接口，且服务条款限制抓取 |
| 2026-09-20 | AT 路线数据来源确定为 NPS/ATC 官方中心线 + OpenStreetMap 兴趣点（庇护所、垭口）+ Census 州界，流水线见 `scripts/` | 都是可公开获取、可标注来源和年份的数据；已实现并有 Python 测试覆盖 |
| 2026-09-20 | 网页版必须通过本地静态服务器运行（如 `python3 -m http.server`），不支持直接双击打开 `index.html` | 页面用 `fetch` 读取 `data/` 下的 JSON，在 `file://` 协议下会被浏览器拦截 |
| 2026-09-20 | 网页端数据存放浏览器 localStorage，导出 JSON 作为备份和跨设备迁移方式 | 符合"本地优先、没有服务器"的原则；已在 `store.js` 实现并测试 |
| 2026-09-20 | 1.0 的进度录入方式确定为手动录入区间（起止里程、日期、地名、备注），重叠区间自动合并去重 | 实现简单、结果准确；已在 `app.js` / `intervals.js` 实现并测试 |
| 2026-09-20 | 地图方案确定为 Mapbox（Outdoors 底图，带等高线和地形） | 已在 `map.js` 实现并验收通过 |
| 2026-09-20 | Mapbox token 不提交到仓库；`js/config.js` 里默认留空，页面首次打开时请求用户输入自己的 token，只保存在该浏览器的 localStorage 里 | 之前仓库里硬编码了作者本人的真实 token，一旦仓库公开就会泄露；对应第 7 节"地图令牌"风险项 |
| 2026-09-20 | 项目正式建立 git 仓库，默认分支 `main` | 开始版本管理，为后续推送到 GitHub 做准备；建库前已清理掉硬编码的 Mapbox token，避免留在提交历史里 |

## 11. 待定事项

| 事项 | 备选或倾向 |
|---|---|
| 开源协议 | 倾向 MIT 或 Apache-2.0，还没有选定，仓库里也还没加 LICENSE 文件 |
| 单位切换（英里 / 公里） | 现在只有英里（`js/app.js` 里硬编码"英里"），公里显示还没做 |
